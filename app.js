"use strict";
const client=window.ferasFloresSupabase;let sound=localStorage.getItem('ff-sound')!=='off';document.addEventListener('DOMContentLoaded',init);
async function init(){setInterval(()=>clock.textContent=new Date().toLocaleTimeString('pt-BR'),1000);clock.textContent=new Date().toLocaleTimeString('pt-BR');soundToggle.textContent=`SOM: ${sound?'LIGADO':'DESLIGADO'}`;soundToggle.onclick=()=>{sound=!sound;localStorage.setItem('ff-sound',sound?'on':'off');soundToggle.textContent=`SOM: ${sound?'LIGADO':'DESLIGADO'}`};searchForm.onsubmit=search;document.querySelectorAll('[data-close]').forEach(x=>x.onclick=()=>modal.hidden=true);await Promise.allSettled([loadSettings(),loadPosts()])}
async function loadSettings(){if(!client)return;const{data}=await client.from('blog_settings').select('setting_key,setting_value').eq('is_public',true);for(const s of data||[]){if(s.setting_key==='site_identity'){heroTitle.childNodes[0].nodeValue=(s.setting_value.hero_title||'PROCURE UM NOME.')+' ';heroAccent.textContent=s.setting_value.hero_accent||'';footerText.textContent=s.setting_value.footer||''}if(s.setting_key==='search'&&s.setting_value.placeholder)searchInput.placeholder=s.setting_value.placeholder}}
async function loadPosts(){if(!client)return;const{data,count}=await client.from('blog_posts').select('id,title,excerpt,published_at,author_name',{count:'exact'}).eq('status','published').order('published_at',{ascending:false}).limit(6);postCount.textContent=count??0;if(!data?.length)return;posts.innerHTML=data.map((p,i)=>`<article><small>${String(i+1).padStart(3,'0')}</small><h3>${esc(p.title)}</h3><p>${esc(p.excerpt||'')}</p><small>${p.published_at?new Date(p.published_at).toLocaleDateString('pt-BR'):''}</small></article>`).join('')}
async function search(e){e.preventDefault();const q=searchInput.value.trim();if(!q)return show('Digite alguma coisa.');if(!client)return show('Arquivo desconectado.');show('CONSULTANDO...');const state=FFProgress.get();state.search_count=(state.search_count||0)+1;FFProgress.save(state);const{data,error}=await client.rpc('resolve_archive_search',{p_query:q,p_player_state:state});if(error)return show('O arquivo não respondeu.');if(!data?.found)return show('Nenhum registro reconheceu essa consulta.');const en=data.entry||{};FFProgress.addFlags(en.unlock_flags||[]);if(data.event)FFEffects.runEvent(data.event);if(data.chat){location.href=`chat.html?chat=${encodeURIComponent(data.chat.chat_key)}`;return}if(data.page){location.href=`secret.html?slug=${encodeURIComponent(data.page.slug)}`;return}if(en.destination_url){location.assign(en.destination_url);return}const item=data.character||data.post||data.resource||en;openResult(en.entry_type,item,en);if(en.response_audio_url)FFEffects.play(en.response_audio_url)}
function openResult(type,item,en){modalType.textContent=(type||'REGISTRO').toUpperCase();modalTitle.textContent=item.name||item.title||en.response_title||'REGISTRO';modalContent.textContent=item.full_content||item.public_description||item.content||item.excerpt||item.description||en.response_message||'';const img=item.portrait_url||item.cover_image_url||en.response_image_url;if(img){modalImage.src=img;modalImage.hidden=false}else modalImage.hidden=true;modal.hidden=false}
function show(t){searchStatus.textContent=t;searchStatus.hidden=false}function esc(v){const d=document.createElement('div');d.textContent=v||'';return d.innerHTML}